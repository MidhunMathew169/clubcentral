<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>forgotPassword</title>
    <style>
        :root {
            --primary-color: #09140d;
            --text-color: #FFFFFF;
            --background-start: #f6f6f6;
            --background-end: #efe3e3;
            --error-color: #ff4444;
            --input-background: #282828;
            --input-text: #FFFFFF;
            --input-border: #404040;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Circular', Helvetica, Arial, sans-serif;
            background: linear-gradient(to bottom right, var(--background-start), var(--background-end));
            color: var(--text-color);
            min-height: 100vh;
        }

        .login-container {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            background: radial-gradient(circle at center, rgba(29, 185, 84, 0.1) 0%, transparent 70%);
        }

        .form-container {
            background: rgb(225 214 214 / 20%);
            padding: 40px;
            border-radius: 10px;
            width: 100%;
            max-width: 400px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px 0 rgb(251 241 241 / 9%);
            margin-top: 40px;
        }

        .form-title {
            text-align: center;
            margin-bottom: 30px;
            color: var(--text-color);
        }

        .form-title h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
        }

        .form-title h3 {
            margin-bottom: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-control {
            background-color: #fef7f703;
            border: 1px solid var(--input-border);
            color: var(--input-text);
            padding: 12px;
            border-radius: 5px;
            width: 100%;
        }

        .form-control:focus {
            background-color: var(--input-background);
            border-color: var(--primary-color);
            color: var(--input-text);
            box-shadow: 0 0 0 2px rgba(29, 185, 84, 0.2);
        }

        .btn-primary {
            background-color: var(--primary-color);
            border: none;
            padding: 12px;
            border-radius: 5px;
            width: 100%;
            font-weight: bold;
            margin-top: 20px;
            transition: transform 0.2s, background-color 0.2s;
        }

        .btn-primary:hover {
            background-color: #1ed760;
            transform: scale(1.02);
        }

        .back-to-login {
            text-align: center;
            margin-top: 20px;
            color: var(--text-color);
        }

        .back-to-login a {
            color: var(--primary-color);
            text-decoration: none;
        }

        .back-to-login a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="form-container">
            <div class="form-title">
                <h2><i class="fas fa-lock mb-3" style="color: #121212;"></i></h2>
                <h3 class="text-dark">Forgot Password?</h3>
                <p class="text-dark">Enter your email to reset your password</p>
            </div>
            <form id="register-form" role="form" autocomplete="off" method="post" action="/forgotPassword">
                <div class="form-group">
                    <input id="email" name="email" placeholder="Email address" class="form-control" type="email" required />
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block">Reset Password</button>
                </div>
                <input type="hidden" name="token" id="token" value="" />
            </form>
            <div class="back-to-login">
                <a href="/login">Back to Login</a>
            </div>
        </div>
    </div>
</body>
</html>